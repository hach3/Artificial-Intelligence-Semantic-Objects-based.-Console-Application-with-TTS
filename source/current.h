#ifndef CURRENT_H
#define CURRENT_H
/*

It's used as a collectioner. That means it creates vectors to save the current concepts in the previous sentence 
It also send this to the memory so it remembers previous conversations

*/
#include <vector>
#include <string>

class Current {
public:
protected:
private:
	
};

#endif //CURRENT_H
